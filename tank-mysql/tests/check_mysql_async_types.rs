use mysql_async::{InfileData, LocalInfileError, WhiteListFsHandler};

#[test]
fn test_imports() {
    let _ = InfileData;
    let _ = LocalInfileError;
    // let _ = WhiteListFsHandler::default(); // assuming it has a new or default or just struct
}
